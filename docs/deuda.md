# Deuda y cabos sueltos

Última actualización: **2026-09-18**

Lo que sabemos que está a medias, mal o sin comprobar. Los bloqueantes primero. Lo saldado se tacha
y se queda.

## Sin comprobar

Lo más caro de esta lista no es lo que está mal, es lo que no sabemos si lo está.

| Elemento | Severidad | Impacto | Estado |
|---|---|---|---|
| ~~No se sincroniza al volver a la ventana~~ | Baja | El plan decía que al volver a la ventana se pediría una pasada. No se hizo en la A2, y con la pasada cada cinco minutos **el cliente veía los cambios del otro Mac solo al cerrar y volver a abrir la bóveda** | **Saldada en la 2.23.2 (2026-09-21)**: al volver a la ventana se pide una pasada —como mucho una cada 30 s— y con la bóveda abierta se mira cada minuto. Probado con dos ventanas abiertas a la vez |
| ~~Con cuenta, la contraseña maestra no se puede cambiar~~ | Media | La contraseña de la cuenta es la maestra de la bóveda, y cambiarla hay que hacerlo en el servidor y en la bóveda a la vez. Cambiándola solo en la bóveda —que es lo que hacía Ajustes—, **un equipo nuevo no podría entrar**. Desde la 2.23.0 se bloquea con un mensaje. **Por eso no se invita a nadie más hasta la A3**, que trae el cambio de verdad y recuperar la cuenta sin ningún equipo a mano | **Saldada en la 2.24.0 (2026-09-21)**: cambia en el servidor y aquí a la vez, y el otro equipo se pone al día al entrar con la nueva sin perder nada |
| La política de privacidad de la cuenta, sin revisar, y sin condiciones de uso | Media | Desde la A2 dice qué se guarda, dónde, quién más interviene —Cloudflare y Resend, empresas de EE. UU. con los datos de las cuentas guardados en la UE— y qué se puede pedir. Está escrita con los hechos del código, **nadie la ha revisado**, y **las condiciones de uso no existen**. Con el registro por invitación el riesgo es pequeño; **antes de abrirlo hay que hacerlo**, y lo hará un modelo especializado ([ADR 0041](adr/0041-los-papeles-de-la-cuenta.md)) | Abierto · antes de la A4 |
| Los topes diarios del servidor no caben en el correo gratuito | Media | Resend en su plan gratuito da **cien correos al día** y `TOPE_ALTAS_DIA` viene en **200**; además cada equipo nuevo, cada recuperación y cada cambio de contraseña gastan otro. Mientras el registro sea por invitación no se llega ni de lejos. **Al abrirlo**, hay que bajar los topes por debajo de cien y **arreglar la frase**: cuando Resend dice que no, el servidor contesta «Prueba otra vez en un momento», y con el cupo agotado eso no es verdad, es hasta mañana ([ADR 0041](adr/0041-los-papeles-de-la-cuenta.md)) | Abierto · antes de la A4 |
| ~~Desde la extensión no se puede recuperar la cuenta~~ | Media | Si se olvida la contraseña maestra y solo se tiene el navegador, **no hay camino**: el panel lo dice y manda a la aplicación, porque recuperar exige volver a envolver la bóveda con una contraseña nueva y eso el núcleo de TypeScript no lo sabe hacer —`boveda.ts` deja fuera crear, cambiar la maestra y rotar la clave de recuperación—. Es coherente con la ADR 0040, pero rompe la promesa de la E2: «con cuenta, la extensión hace todo sin la aplicación». Cerrarlo es implementar en el núcleo el cambio de maestra y en el cliente las tres rutas de recuperación (revisión de los textos, 2026-09-23) | **Aceptado (2026-09-23)**: se le preguntó al cliente y eligió que se mande a la aplicación. Queda dicho en el panel y matizada la ADR 0040 |
| ~~Exportar y borrar la cuenta entera, solo por correo~~ | Baja | El servidor ya sabe hacer las dos cosas (`/v1/cuenta/exportacion`, `DELETE /v1/cuenta`), pero la ventana no tiene los botones: la política dice que se pidan en info@webcafeina.com mientras tanto | **Saldada en la 2.24.0 (2026-09-21)**: los dos botones están en Ajustes, y la política de privacidad ya no remite al correo |
| ~~La prueba «la clave de recuperación abre la bóveda» falla de vez en cuando~~ | Media | Tras «Cerrar la bóveda», la interfaz volvía a pedir la lista (`BuscarEnBoveda`) cuando ya estaba cerrada: la búsqueda espera 120 ms a que se deje de teclear, y si en ese hueco se cerraba, salía igual. **Ya pasaba antes de la A1** (1 de cada 12 con el código de la 2.22.2) | **Saldada el 2026-09-18**: al cerrar, la pantalla deja de preguntar (`cerrada` en `boveda.tsx`). 24 de 24 seguidas en verde |
| Una nota editada a la vez en dos equipos pierde un lado | Baja | La fusión junta campo a campo y, si el mismo campo cambió en los dos, gana la entrada «mayor». Con la contraseña no se pierde nada —la otra va al historial—, pero **las notas no tienen historial** ([ADR 0038](adr/0038-sincronizar-la-boveda.md)) | Aceptado · se ve con el uso |
| Una entrada borrada puede volver | Baja | Si otro equipo la editó a la vez —la edición gana al borrado— o si pasa más de seis meses sin conectarse y la lápida ya se fue. Se vuelve a borrar y ya ([ADR 0038](adr/0038-sincronizar-la-boveda.md)) | Aceptado |
| El correo se normaliza con dos minúsculas distintas | Baja | `NormalizarCorreo` en Go y `normalizarCorreo` en el servidor pasan a minúsculas con las reglas de dos lenguajes. En correos corrientes coinciden; en algún caso raro de Unicode podrían no hacerlo. Manda la del servidor ([ADR 0037](adr/0037-claves-de-la-cuenta.md)) | Abierto |
| ~~**El servidor de cuentas, sin desplegar**~~ | Media | Escrito y probado entero dentro de `workerd` (39 pruebas, ADR 0036), pero nada había corrido en Cloudflare: ni la jurisdicción `eu`, ni el dominio propio, ni los correos | **Saldada el 2026-09-18**: desplegados los dos Workers, las bases D1 en la UE leídas en la API, los objetos con jurisdicción funcionando, y el correo en la bandeja de entrada con SPF, DKIM y DMARC en `PASS`. Queda sin ver el freno por IP de Cloudflare cortando |
| La pimienta del servidor no se puede rotar | Baja | Los verificadores van firmados con `PIMIENTA` y cada cuenta guarda qué versión de la pimienta usó (`pimienta: 1`), pero **no hay código que rote**: cambiarla hoy deja fuera a todas las cuentas. Si se filtra, hay que escribirlo antes de cambiarla ([ADR 0036](adr/0036-el-servidor-de-cuentas.md)) | Abierto · **y vencido**: la condición era «antes de abrir el registro», y el registro se abrió el 2026-09-23 sin esto. Sube a lo primero de la lista de servidor |
| ~~La extensión de la Chrome Web Store no encuentra Esfinge hasta la 2.22.2~~ | Baja | Su identificador (`jfkkegampjamnnlopobepjoanebemegp`) está en `extensionesDeChrome` desde el 2026-09-15 pero **sin publicar**: el cliente decidió esperar a que terminen las dos revisiones para no mandar a Firefox una versión idéntica. Si Chrome la aprueba antes, quien la instale desde la tienda verá «No se encuentra Esfinge» ([ADR 0033](adr/0033-las-tiendas.md)) | **Saldada con la 2.22.2 (2026-09-18)** |
| Cada publicación sube la extensión a las tiendas, aunque no haya cambiado | Baja | El trabajo `tiendas` sube en cada etiqueta. Una versión que solo toca la aplicación manda a revisar una extensión idéntica, y en Chrome una subida nueva durante una revisión puede volver a empezarla. Se propuso subir **solo cuando cambie el código de la extensión** y el cliente decidió seguir subiendo siempre (2026-09-18) ([ADR 0033](adr/0033-las-tiendas.md)) | Aceptado · cada versión se revisa |
| ~~La subida a las tiendas no se ha probado contra las tiendas~~ | Media | El trabajo `tiendas` y `herramientas/tienda-chrome.mjs` están escritos con la documentación oficial de las dos tiendas, pero **no se han ejecutado nunca contra ellas**: faltan las cuentas. Dos cosas en concreto: los campos que devuelve la API v2 de Chrome al subir y que **AMO acepte `es-ES`** como idioma de la ficha en la primera subida ([ADR 0033](adr/0033-las-tiendas.md)) | **Saldada (2026-09-18, 2.22.2)**: Firefox con `web-ext` desde la 2.22.1, y Chrome por la API v2 con la cuenta de servicio, a la primera: subida `SUCCEEDED` y enviada a revisión (`PENDING_REVIEW`) |
| Las capturas de la portada son de la 2.0.3 | Baja | `docs/imagenes/portada-{claro,oscuro}.png`, las del README, enseñan la ventana antes de la bóveda: barra lateral con cuatro secciones y la versión 2.0.3 al pie. Por eso **la web del proyecto no lleva captura** (ADR 0033). Rehacerlas pide el servidor de desarrollo y una bóveda de ejemplo sin datos de nadie | Abierto |
| La compilación reproducible, con el Node de los revisores de Mozilla | Baja | Mozilla compila el código fuente de cada versión y lo compara byte a byte. Aquí sale idéntico con Node 22 y pnpm 11.20.0 en una carpeta limpia; **sus revisores usan Node 24 por defecto**, y `COMPILAR.md` pide el 22. Si un día difiere, rechazan la versión ([ADR 0033](adr/0033-las-tiendas.md)) | Abierto · se ve con la primera revisión |
| Una extensión recién instalada no rellena hasta abrir el panel | Baja | Lo pide la tienda de Chrome —aviso y aceptación dentro de la extensión— y el cliente eligió pedirlo en el panel y no en una pestaña al instalar. Hasta abrirlo, el icono enseña «!» y «Abre el panel para empezar». Si con el uso no se entiende, la salida es abrir el aviso en una pestaña al instalar ([ADR 0033](adr/0033-las-tiendas.md)) | Aceptado · se ve con el uso |
| **La extensión en un Windows de verdad** | Media | Desde la 2.22.0 Esfinge escribe el registro y el instalador lleva el puente, pero **ningún navegador lo ha lanzado nunca en Windows**: ni Chrome, ni Edge, ni Firefox. Tampoco se sabe qué clave leen Brave, Vivaldi y Opera, ni se ha visto el puente sin consola (`-H windowsgui`) hablando por la entrada y la salida estándar ([ADR 0034](adr/0034-el-canal-con-el-navegador-en-windows.md)) | Abierto · depende de que alguien lo pruebe |
| La aplicación no se ha ejecutado en Windows ni en Linux | Media | En macOS está probada de sobra —diálogos, arrastrar y soltar, doble clic en un `.esf`, menús, estructura y vidrio—, pero en los otros dos sistemas todo lo visual sigue siendo una suposición | Abierto · depende del humano. Bajó de Alta a Media cuando la 2.10.0 cerró el frente de macOS |
| El `.deb` no se puede instalar aquí | Media | Se puede inspeccionar con `dpkg -c`, pero instalarlo exige permisos que esta máquina no da | Abierto |
| El instalador de Windows | Media | Se compila, pero nadie lo ha ejecutado | Abierto |
| Los iconos con la bóveda de verdad | Baja | Medido contra doce dominios conocidos: **nueve dan icono**. Los que faltan lo declaran en el HTML, que sigue descartado. Cuántos de los 65 del cliente lo darán es otra cosa, y de eso depende si algún día hay que leer el HTML ([ADR 0024](adr/0024-iconos-de-los-sitios.md)) | Abierto · depende del humano |
| ~~El canal del navegador, comprobado solo en Firefox~~ | Baja | **Comprobado en las dos familias** (2026-09-10): Firefox en la 2.17.6 y Chrome en la 2.17.7, las dos de punta a punta en un Mac. Lo que queda es que **el identificador de la tienda será otro** cuando se suba: se añade al lado de éste y ya | Saldada · el de la tienda, `jfkkegampjamnnlopobepjoanebemegp`, añadido el 2026-09-15 |
| **Nada prueba el trabajador de fondo ni `pagina.ts` con la extensión cargada** | **Alta** | Está probado en Chromium lo que decide: la detección de campos, el panel compilado, **qué enseña el icono** (`insignia.ts`, función pura) y **el filete y el aviso** (`marcas.ts`). Lo que sigue sin probar es lo que lo envuelve, y desde la 2.20.0 es más —y desde la 2.22.0, **el bloqueo de los puertos sin el aviso de datos aceptado** y que la página empiece al aceptarlo sin recargar—: los eventos de pestaña y **la alarma de cada minuto** que ponen al día el icono, el guardián de tramas y el observador de la página. **Y desde la entrega 3, la contraseña pendiente cruzando la navegación** en la memoria del trabajador, y la detección del envío en páginas de verdad ([ADR 0032](adr/0032-guardar-desde-la-pagina.md)). Ahí vivía el otro fallo de la 2.18.0. La salida sigue siendo **Playwright con la extensión cargada** (`launchPersistentContext` con `--load-extension`) y un host de native messaging de mentira | Abierto · el panel, la insignia y las marcas, saldados el 2026-09-14. **Saldado en parte el 2026-09-22 (E2)**: `navegador/pruebas-reales` carga la extensión de verdad **con cuenta** y ejercita el trabajador de fondo, el guion de la página rellenando, las alarmas y el panel. Sigue sin probar así **el camino por el canal con la aplicación** (haría falta el host de mentira) y **la tarjeta de guardar cruzando una navegación**: va en una sombra cerrada que Playwright no puede pulsar |
| Rellenar en sitios de verdad | Media | **Comprobado en un Mac (2026-09-10, 2.18.1): rellena solo al cargar y acierta el formulario en `login.brevo.com` y `dash.cloudflare.com`, en Chrome y en Firefox**, y el botón del panel escribe tras borrar los campos a mano. Bajó de Alta a Media con eso. Lo que sigue sin saberse solo lo dice el uso: **un sitio difícil** —un banco, donde los formularios son más raros—, si rellenar solo resulta demasiado —y entonces hace falta un interruptor en Ajustes, no afinar a ciegas— y si un clic más en el panel molesta cuando hay varias cuentas del mismo sitio ([ADR 0028](adr/0028-rellenar-en-la-pagina.md)) | Abierto · depende del humano |
| ~~Guardar y actualizar desde la página en sitios de verdad~~ | **Alta** | **Comprobada por el cliente en su Mac con la 2.21.2 (2026-09-14): los ocho pasos funcionan**, y Google por los dos caminos. Antes, **Probada por el cliente con la 2.21.0 hasta cambiar la contraseña** (2026-09-14): en Google, con la página del usuario y la de la contraseña separadas, **rellenaba y ofrecía actualizar la cuenta de otro usuario**; corregido en la 2.21.1 recordando el usuario de la página anterior ([ADR 0032](adr/0032-guardar-desde-la-pagina.md)). Escrito y probado aquí por piezas —qué es cada formulario, cuándo caduca un pendiente, la tarjeta, y en Go la tubería entera— pero **nadie ha enviado un formulario de verdad con la extensión cargada**. Hay que ver en el Mac, en Firefox y en Chrome: entrar con una cuenta nueva, entrar con otra contraseña, registrarse, cambiar la contraseña, un inicio fallido, «Nunca en este sitio» y deshacerlo, y que la ventana se ponga al día sola ([ADR 0032](adr/0032-guardar-desde-la-pagina.md)) | **Saldada (2026-09-14)**, Brevo incluido |
| ~~El selector de cuentas de Google~~ | Media | Cuando Google enseña «Elige una cuenta» y se pulsa una ficha, **no hay campo de correo** que leer, y la página de la contraseña no declara ningún `username` ([ADR 0032](adr/0032-guardar-desde-la-pagina.md)) | **Saldada en la 2.21.1 (2026-09-14), sin comprobar en Google**: el diagnóstico por la consola del cliente enseñó que esa página lleva el correo en un `type="email"` escondido con `autocomplete="off"`, y ahora cuenta como usuario si hay una contraseña a la vista y un solo correo. La página copiada está en las pruebas |
| Sin saber el usuario, se ofrece actualizar las cuentas del sitio | Media | Si al enviar no hay usuario en el formulario ni se tecleó en la página anterior —porque se dejó el que puso Esfinge, o el sitio no lo declara—, `ofrecer` propone actualizar las cuentas que haya. Con el que puso Esfinge es lo correcto; con un sitio que no declara el usuario puede ser la cuenta de otro. **La tarjeta enseña qué cuenta se actualiza**, y la contraseña anterior va al historial | Abierto · se ve con el uso |
| «La contraseña era mala» es una heurística | Media | Tras enviar, **si vuelve a salir un campo de contraseña se da por fallido** y no se ofrece. Falla en los dos sentidos: un sitio que tras entrar enseña otro formulario con contraseña no ofrecerá nada, y uno que dice «contraseña incorrecta» sin volver a pintar el campo ofrecería guardar la mala. El segundo es el caro, y por eso, al registrarse o cambiar, solo cuenta si el campo sigue relleno. Se afina **con los casos que salgan**, no a ciegas. **Y un tercero**: un sitio que cambia la contraseña sin cambiar de página y **deja los campos rellenos** tras un cambio bien hecho no ofrecerá nada. Brevo no es uno de ellos: con la 2.21.2 ofrece al cambiar, comprobado por el cliente | Abierto · se ve con el uso |
| Un formulario en un marco de otro origen no ofrece guardar | Baja | Igual que no se rellena ahí: el guion no corre en tramas ajenas. Algunos inicios de sesión viven en un `iframe` de otro dominio | Aceptado · ADR 0028 y 0032 |
| La oferta se pierde si el navegador mata el trabajador | Baja | MV3 cierra el trabajador de fondo cuando le parece, y el pendiente vive solo en su memoria. Guardarlo en `storage.session` lo evitaría a costa de dejar la contraseña en un almacén; se prefirió perder alguna oferta | Aceptado · ADR 0032 |
| La lista de sitios excluidos en Ajustes no tiene prueba de interfaz | Baja | Se lee con la bóveda abierta y se quita con «Quitar». Go está probado; la lista de la ventana no la ejercita `make e2e` | Abierto |
| Rellenar el código en otros formularios | Baja | **Comprobado en Cloudflare con la 2.19.1, en Firefox y en Chrome** (2026-09-14), después de que la 2.19.0 no reconociera sus casillas. Bajó de Media a Baja con eso. Lo que queda son **otros formularios de segundo factor**: los que solo reaccionan a pegar o a pulsaciones de tecla no se rellenarán, aunque Esfinge **lo dice** en vez de contar que ha ido bien ([ADR 0030](adr/0030-rellenar-el-codigo-de-un-solo-uso.md)) | Abierto · se ve con el uso |
| ~~El icono con estados y la marca en el campo, en el Mac~~ | Baja | **Comprobado (2026-09-14, 2.20.2 y 2.20.3)**: el cliente lo usó en Firefox y en Chrome y dice que funciona y se ve todo bien, después de cinco correcciones visuales en tres versiones. Lo que no ha dicho expresamente —si el candado aparece solo al minuto de cerrarse la bóveda y si el correo cabe entero en el panel— se da por visto con el uso, no por comprobado ([ADR 0031](adr/0031-el-icono-con-estados-y-la-marca-en-el-campo.md)) | Saldada · quedan esos dos detalles para el uso |
| El permiso de sitio en Firefox | Baja | En MV3 los `host_permissions` **no se conceden al instalar**. La extensión funciona en un Firefox de verdad (2026-09-10, 2.18.1), así que no es un bloqueo; lo que **no** se apuntó es si hubo que concederlo a mano o vino dado. Importa para las instrucciones de instalación cuando esto vaya a AMO. Si falta, el panel lo dice con una frase que explica dónde darlo | Abierto · pendiente de apuntar al instalarla de nuevo |
| El portapapeles en Windows y Linux | Baja | Va por la API del navegador dentro del webview; en macOS está comprobado. Desde la bóveda, además, **el borrado pasa por Go** (`Sistema.PonerEnPortapapeles`), que en Wails usa la API del sistema | Abierto |
| La bóveda con datos de verdad | Baja | **Comprobado (2026-09-09)**: en un Mac, con una exportación real de Dashlane que entra entera —los cuatro ficheros— y con la clave de recuperación abriendo la bóveda, que era lo único que ninguna prueba podía decir. **Y (2026-09-10) el código de un solo uso coincide con el de Dashlane**, con los dos abiertos a la vez, que era lo mismo para los segundos factores: los vectores del RFC dicen que el algoritmo está bien, no que la semilla importada sea la buena. Lo que queda no es una comprobación sino **uso**: vivir con ella lo bastante para saber si los plazos estorban y si se abre Esfinge o se sigue abriendo Dashlane. De eso depende seguir o parar ([ADR 0023](adr/0023-la-boveda.md)) | Abierto · depende del humano |
| ~~El Worker de pruebas es público, y su buzón también~~ | Media | `esfinge-cuentas-pruebas` está en `workers.dev` con `ENTORNO=pruebas`, así que `/_pruebas/buzon?correo=…` **entrega los códigos de cualquier cuenta de ese servidor a quien pregunte**. No toca producción —otro Worker, otra base, otros secretos—, pero cualquiera puede darse de alta ahí y, si alguien usa una dirección de verdad, leerle el código. La salida es ponerle Cloudflare Access delante, y de paso confirmar que sus secretos no son los de producción (revisión del 2026-09-23) | **Saldada el 2026-09-23 por la tarde**: el cliente creó en Zero Trust una aplicación autoalojada **sobre la ruta**, no sobre el Worker —`esfinge-cuentas-pruebas.webcafe-na.workers.dev/_pruebas/buzon`—, con política «Allow · Emails ending in `@webcafeina.com`». Comprobado desde fuera: el buzón contesta 302 a `webcafeina.cloudflareaccess.com`, también con barra final, barra doble y en mayúsculas, y el que no encaja con la ruta lo rechaza el propio Worker con 404. `/v1/salud` y `/v1/prelogin` siguen contestando 200, que era la condición: proteger el Worker entero habría cerrado también la API. **Y abre**: el cliente se identificó con el código de su correo y vio el buzón, así que la puerta funciona en los dos sentidos |
| Un envío a un correo sin cuenta se pierde en silencio | Media | Para no delatar quién tiene cuenta, `POST /v1/llaves/de` devuelve unas llaves **inventadas pero fijas** a un correo sin cuenta, y `POST /v1/envios` contesta `202` igual. Quien mande ahí cifra hacia una llave que no abre nadie y **el sobre se tira**: cree que ha llegado. Es deliberado y temporal —lo arregla la **B3**, las invitaciones—, pero mientras tanto no se puede publicar compartir (ADR 0043) | Abierto · lo cierra la B3 |
| El servidor puede congelar o bifurcar la versión de la bóveda | Baja | La versión es un número y no una cadena que ate cada documento al anterior: un servidor malicioso puede dejar a un equipo en la versión que ya tiene, o servirle a cada uno su propia rama, sin que ninguno se entere. **No puede inventarse contenido.** Se cerraría sellando la huella del documento anterior, que es una subida de formato. Dicho en `docs/seguridad.md` (revisión del 2026-09-23) | Abierto · no urge |
| Los plazos de la papelera y de las lápidas los decide el reloj de cada equipo | Baja | Se purgan al abrir, así que un ordenador con la hora muy adelantada purga para toda la cuenta —treinta días la papelera, ciento ochenta las lápidas—. No hace falta mala fe. Arreglarlo pide una hora de referencia que hoy no hay (revisión del 2026-09-23) | Abierto |
| El nombre del equipo va dentro del correo que recibe el dueño | Baja | Lo escribe quien entra: ochenta caracteres a su gusto, sin saltos de línea ni caracteres de control, en un correo de texto —no de HTML— y siempre entre comillas. Quien pueda ponerlo ya sabe la contraseña maestra, así que lo que se gana engañando ahí es poco; queda dicho por si alguien lo ve raro (revisión del 2026-09-23) | Aceptado |

## Técnica

| Elemento | Severidad | Impacto | Estado |
|---|---|---|---|
| Con cuenta, **copiar desde el panel no se borra solo del portapapeles** | Media | La aplicación copia ella y lo borra pasado el plazo; con cuenta la bóveda está en el trabajador de fondo, que no puede tocar el portapapeles, y copia el panel, que se cierra enseguida (ADR 0040). El panel lo dice al copiar, y está en `docs/seguridad.md` | Abierta · la salida sería un documento fuera de pantalla (`offscreen`) en Chrome y la página de fondo en Firefox, con un permiso más; se decide con el uso |
| **Firefox no se prueba con la extensión cargada** | Media | La prueba de `navegador/pruebas-reales` es solo Chromium: Playwright no carga extensiones en Firefox. Lo que difiere —`storage.session`, la página de fondo en vez del trabajador, el WebAssembly bajo su CSP— solo se ve en su Mac (E3) | Abierta |
| El plazo de bloqueo de la extensión con cuenta **no se puede cambiar** | Baja | Quince minutos, como la aplicación por defecto, y al cerrar el navegador. La extensión no tiene Ajustes | Abierta · se decide con el uso |
| La lista de sufijos públicos de la extensión (`tldts`) y la de Go (`x/net`) **son dos copias con fechas distintas** | Baja | Un sufijo muy reciente puede estar en una y no en la otra, y entonces la aplicación y la extensión decidirían distinto qué cuentas son de un sitio. La prueba cruzada mira los casos que importan, no la lista entera | Aceptada · se actualizan las dos al subir versiones |
| ~~Un fallo en la puerta de publicación no dejaba nada que mirar~~ | Media | El informe de Playwright no se generaba en la máquina de GitHub, así que el artefacto `informe-pruebas` venía vacío («no valid artifacts found») y del fallo solo quedaban tres líneas. Con una prueba que falla allí y no aquí, eso es adivinar | **Saldada (2026-09-23, 2.25.4)**: con `CI`, informe HTML, traza y captura al fallar, y el artefacto se lleva también `test-results/` |
| La extensión da por subido lo que fundió al volver a entrar | Baja | Si la bóveda de aquí tenía cambios que el servidor no tiene, la fusión de la entrada los conserva pero apunta el recuerdo como si estuvieran subidos: el estado dice «al día» con cambios sin subir hasta el siguiente guardado o el siguiente cambio remoto. **No se pierde nada** —la fusión a tres bandas lo rescata—, pero el estado miente. Salió en la revisión del 2026-09-23 | Abierta · guardar el recuerdo con la serie a -1 cuando la fusión diga que hay que subir |
| La prueba de dos ventanas se queda en «Sincronizando…» en la máquina de GitHub | Media | Falló dos veces seguidas en la puerta de la 2.25.4 y **nunca aquí**, ni con la máquina cargada a propósito. De la primera salió un fallo de verdad —el vigilante dormido tras encontrar el turno cogido—, pero no era la causa: sigue pasando. Ahora la prueba espera más de lo que tarda la red en rendirse y **enseña lo que dice Go**, no solo el texto de la línea, para saber si es lentitud, una petición colgada o una pasada cancelada en silencio | Abierta · esperando a que vuelva a fallar con el estado a la vista |
| La prueba de dos ventanas falló una vez de siete con «Network connection lost» en la primera petición al servidor de cuentas local | Baja | `wrangler dev` cortó la conexión del alta recién arrancado (2026-09-22). No es de Esfinge, pero si se repite en la puerta de publicación parará una versión sin motivo | Abierta · si vuelve, esperar a que el Worker conteste algo de verdad antes de empezar, no solo `salud` |
| ~~Con la contraseña nueva en «Abrir la bóveda», un equipo sin testigo de confianza no la reconoce~~ | Media | Da «Esa llave no abre esta bóveda», como si fuera una contraseña mala, y hay que dar con «¿Cambiaste la contraseña en otro equipo?». Pasa una vez en cada Mac que venía de la 2.24.0 sin haberse abierto con la 2.24.1 —el testigo iba sellado—, **y volverá cada vez que caduque el testigo, a los 90 días**. Lo vio el cliente el 2026-09-21 | **Saldada (2026-09-21, 2.24.2)**: se reconoce igual y se pide el código en la misma pantalla |
| El `main.go` de la aplicación vive en la raíz, no en `cmd/` | Baja | Rompe la organización idiomática de Go. Lo impone Wails, que busca el paquete main junto a `wails.json` | Aceptado · [ADR 0006](adr/0006-de-terminal-a-ventana.md) |
| La interfaz construida se copia a `internal/interfaz/dist` | Baja | Un paso más en la compilación. `go:embed` no puede salir del directorio de su paquete | Aceptado |
| El servidor de desarrollo publica los métodos por reflexión | Baja | Si un método cambia de firma, el fallo sale en tiempo de ejecución y no al compilar | Aceptado · solo existe tras la etiqueta `dev` |
| ~~Una dependencia de la compilación tumba una publicación al primer hipo~~ | Media | Cuatro publicaciones caídas ya por un tercero con un mal minuto, con todo lo nuestro en verde: un índice de apt caducado, Chocolatey con 503 dos veces, y la 2.18.0 con `go install …/wails` sin poder hablar con `sum.golang.org` («TLS handshake timeout») | **Saldada (2026-09-10)**: `herramientas/reintentar.sh` envuelve las cinco instalaciones de herramientas de los dos flujos, con espera creciente. **Sin bajar la guardia**: nada de saltarse la base de datos de sumas ni de `--insecure`; se reintenta lo mismo |
| ~~La puerta de publicación no comprobaba la extensión~~ | **Alta** | `make comprobar` sí miraba los tipos y —desde la 2.17.6— que el manifiesto declare lo que el código usa, pero la puerta de `publicar.yml` **repite los pasos a mano** en vez de llamar a `make`, y ese trozo nunca se copió: la comprobación que existe para que no vuelva a faltar una palabra en una lista **no la ejecutaba ninguna publicación**. Es el agujero de los vectores del formato con otra cara | **Saldada (2026-09-10)**: la puerta corre `pnpm run comprobar` de `navegador/`, que incluye las pruebas de qué campo se rellena. La 2.18.0 salió con la puerta todavía sin ese paso; se corrió a mano y en verde |
| La prueba de la clave de recuperación falla a veces | Baja | **Dos veces ya**: el 2026-09-10 y el 2026-09-14, las dos en tema claro, con el mismo error —un `400` en la consola, que en el servidor de desarrollo es **cualquier método de Go devolviendo un error**, y la prueba exige la consola limpia— y las dos en verde al repetir la tanda entera. La segunda coincidió con un cambio en `componentes.tsx`, y por eso se repitió la tanda completa antes de darla por intermitente. Las pruebas comparten una bóveda dentro de la tanda, así que el resultado depende del orden. Si sube de una de cada tres tandas, lo que hay que quitar es la dependencia del orden, no la aserción | Abierto |
| No hay pruebas de la línea de comandos | Media | `internal/cli` no tenía ni un test: se comprobaba a mano en cada cambio | **Parcialmente saldada (2026-09-09)**: `conSalida` sí tiene pruebas —escribe con 0600, no pisa sin `--forzar`, no deja el fichero a medias, acepta `/dev/null`—, porque se le movieron las tripas a `internal/escritura` y hacer eso sin red es como se rompen las cosas en silencio. El resto de subcomandos sigue sin cubrir |
| ~~Los códigos de un solo uso (TOTP) no se calculan~~ | Media | La bóveda guardaba la semilla y la traía al importar, pero nadie calculaba el código de seis cifras: se enseñaba la semilla, que no sirve para entrar en ningún sitio | **Saldada en la 2.15.0**: `internal/codigos`, sin dependencias, con los vectores de RFC 4226 y RFC 6238 enteros. En la ventana con su cuenta atrás y en `esfinge boveda codigo` ([ADR 0025](adr/0025-los-codigos-de-un-solo-uso.md)). Lo que no puede decir ninguna prueba: si abre una cuenta de verdad |
| ~~El freno del canal no frenaba nada~~ | **Alta** | El contador de preguntas por minuto vivía **en la conexión**, y la extensión abre **una conexión por petición** —lo pide MV3, que mata su trabajador cada pocos minutos—. Cada pregunta llegaba con el contador a cero, así que el tope de sesenta que impedía reconstruir la lista de sitios de la bóveda con un diccionario de dominios **no se alcanzaba jamás**. Y la prueba estaba en verde porque le pasaba un contador hecho a mano a sesenta llamadas seguidas, que es el caso que no ocurre | **Saldada en la entrega 2**: los frenos cuelgan del servidor, no hay dónde poner uno por conexión, y la prueba abre una conexión por pregunta. Misma familia que los iconos: probar el camino real, no el cómodo |
| ~~En Windows el manifiesto del navegador va al registro, y no se escribe~~ | Media | En macOS y Linux Esfinge deja un fichero JSON en la carpeta de cada navegador al encender el canal; en Windows eso son claves de registro bajo `HKCU`, que es otro código. Sin él, la extensión **no puede lanzar el puente** allí aunque el socket funcione | **Saldada (2026-09-14)** ([ADR 0034](adr/0034-el-canal-con-el-navegador-en-windows.md)): manifiestos en `%APPDATA%\Esfinge` y una clave por navegador, probado con un registro de mentira aquí y con el de verdad en la máquina Windows de GitHub, en verde con la 2.22.0. **Sin probar con un navegador en un Windows de verdad** |
| ~~El instalador de Windows no instala el puente del navegador~~ | Baja | El binario se publica para los seis objetivos, pero el instalador solo copia la aplicación: en Windows hay que dejarlo a mano | **Saldada (2026-09-14)**: plantilla NSIS propia (`build/windows/installer/project.nsi`) con el puente, y la publicación comprueba que va dentro ([ADR 0034](adr/0034-el-canal-con-el-navegador-en-windows.md)) |
| `personalinfo.csv` de Dashlane no se importa | Baja | Direcciones, teléfonos y fechas de nacimiento. No son secretos sino datos de autorrelleno, así que meterlos en una bóveda es una decisión de producto por tomar y no un fallo del importador. Si se intenta, el fichero se rechaza diciendo qué columnas trae | Abierto |
| ~~La papelera no se puede vaciar desde la ventana~~ | Media | Borrar es borrado suave —hace falta para sincronizar después— así que una entrada borrada se quedaba en el fichero para siempre, y encima el borrado **no tenía vuelta atrás**: dos clics y la contraseña se había ido | **Saldada en la 2.16.0**: papelera de verdad ([ADR 0026](adr/0026-la-papelera.md)), con restaurar, borrar del todo, vaciar a mano y vaciado solo a los treinta días. El coste, dicho en `docs/seguridad.md`: durante esos días la contraseña borrada sigue dentro del fichero |
| El JSON exterior de la bóveda no va autenticado en su conjunto | Baja | Quien pueda escribir el fichero no puede leer nada ni fabricar una bóveda que abra, pero sí estropearla o revertirla a una copia vieja. `comprobarCoherencia` lo **detecta** con un sello por dentro; no lo impide | Aceptado · [ADR 0023](adr/0023-la-boveda.md) |
| Cifrar una tanda deriva la clave una vez por fichero | Baja | Es el precio de que cada contenedor lleve su sal, y no se va a quitar: compartir la derivación entre ficheros sería compartir la sal | Aceptado. Lo que sí se hizo es paralelizarlo, con tope de la mitad de los núcleos y máximo cuatro: veinte ficheros pasaron de 4,42 s a 1,29 s ([ADR 0018](adr/0018-tandas-en-paralelo.md)) |

## De producto

| Elemento | Severidad | Impacto | Estado |
|---|---|---|---|
| ~~La comprobación de actualizaciones solo ocurre al arrancar~~ | Media | `comprobarAlArrancar` la llamaba `Arrancar` **una sola vez** y no había ningún reloj: con Esfinge abierta no volvía a mirar nunca, mientras la portada y la [ADR 0014](adr/0014-comprobacion-de-actualizaciones.md) prometían «una vez al día». Lo dijo el cliente: nunca le había salido la banda de aviso | **Saldada en la 2.10.4**: `vigilar` deja un reloj que se asoma cada hora y aplica la misma puerta de las 24 h. Tres pruebas con `-race`: que avisa sin reiniciar, que el techo de una petición al día se mantiene, y que el reloj se para al cerrar la ventana (2026-09-08) |
| No hay desplegable de Esfinge dentro del campo | Media | Con **una** cuenta guardada del sitio se rellena sola y no hace falta elegir nada; con **varias** no se toca la página y hay que elegir en el panel, que es un clic más. Se decidió así a propósito ([ADR 0028](adr/0028-rellenar-en-la-pagina.md)): un desplegable propio dentro de la página de otro obliga a un marco de nuestro origen y a pelearse con el `z-index` de cada sitio, y eso es superficie en todas las páginas. Se decide **con el uso**, no con la intuición: si el clic de más aparece a menudo, se hace | Abierto · depende del humano |
| `frontend/public/icono-256.png` no lo usa nadie | Baja | Se genera, se copia a `dist/` y se embebe en el binario, y no lo referencia ni una línea de código. La marca de dentro de la ventana sale de `build/marca.svg`, así que este PNG solo vale como favicon —que en un webview de Wails tampoco se pinta—. O se usa o sobra | Abierto |
| `VersionVista` se escribe y no se lee | Baja | Se guarda en las preferencias «para no repetir el mismo aviso», y no hay ni un sitio que la consulte: el descarte de la banda es solo de la interfaz y no sobrevive al reinicio. O se usa o sobra | Abierto |
| Gatekeeper avisa en macOS y Windows | Baja | El cliente ve un aviso de programa no identificado. Firmar cuesta 99 $/año y se decidió no hacerlo | Aceptado · [ADR 0012](adr/0012-sin-firmar.md). Bajó de Media a Baja al comprobarse que es **solo de la primera instalación**: al actualizarse desde dentro no aparece, porque la cuarentena la pone quien descarga y ahí descarga Go (2026-09-08) |
| ~~El icono del documento `.esf` era el de la aplicación~~ | Baja | Un fichero cifrado y el programa que lo abre se veían igual: `build/esf.png` era una copia byte a byte de `build/appicon.png`, y en Linux el `.deb` apuntaba al icono de la aplicación | **Saldada (2026-09-08)**: `build/documento.svg` dibuja una hoja con la esquina doblada y la marca sobre una placa. **Comprobado en el Mac**, y la marca se centró en la hoja tras verlo puesto (2.10.2). En Windows sale del mismo PNG sin tocar nada; en Linux hizo falta instalar `application-x-esfinge.png` en `mimetypes/`, comprobado con `dpkg -c` (2.10.3). Sin ver todavía en Windows ni en GNOME de verdad |
| ~~No hay barra de menús propia~~ | Baja | Sin atajos de teclado ni órdenes en la barra del sistema | **Saldada**: se construye entera y en español en los tres sistemas, con atajos ⌘1…⌘6. Los roles de Wails no servían porque traen los rótulos en inglés escritos a fuego, así que las acciones de edición las hace la ventana con una orden ([ADR 0015](adr/0015-menus-en-espanol.md)). Comprobado en el Mac, que era donde más riesgo había (2026-09-07) |

## Saldada

- ~~NSIS se bajaba de Chocolatey en cada publicación~~ → su servidor devolvió 503 durante media hora y
  tiró **dos publicaciones seguidas** con todo lo demás en verde. Se probó primero a usar el
  preinstalado de la imagen de Windows, y **la suposición era falsa: esa imagen no lo trae** —lo dijo
  el propio registro—. Así que se baja del proyecto NSIS directamente, con su suma comprobada y sin
  gestor de paquetes por medio. Se deja también la búsqueda del preinstalado, que no estorba y sirve
  si algún día lo incluyen (2026-09-10).
- ~~El manifiesto de la extensión no declaraba el permiso `storage`~~ → **y ésta es la buena**: el
  código llamaba a `api.storage.local` para guardar el testigo, el permiso no estaba, `api.storage`
  era `undefined` y la excepción saltaba en la **primera línea** del trabajador de fondo. Nadie la
  recogía, así que el trabajador no contestaba y desde fuera se veía como si el puente con Esfinge no
  respondiera. Se persiguió el puente, el socket, los manifiestos de native messaging, el modelo de
  mensajes entre navegadores y hasta el orden de bytes: **cinco versiones publicadas por una palabra
  que faltaba en una lista**. Lo encontró la consola de la extensión, que es lo primero que había que
  haber mirado. Ahora hay un guardián (`navegador/herramientas/permisos.mjs`) en `make comprobar`
  (2026-09-10, 2.17.5). → decirle a Chrome que la
  respuesta llega después es `return true` y una retrollamada; decírselo a Firefox es devolver la
  promesa. Con la forma de Chrome, Firefox tira la respuesta y el panel no recibe nada. Se habla por
  un **puerto**, que no promete nada y es igual en los dos (2026-09-10, 2.17.3).
- ~~En macOS no se le escribía el manifiesto a Firefox~~ → para saber si estaba instalado se miraba
  **la carpeta de destino** y no la del navegador, y en macOS no son la misma: el perfil vive en
  `…/Application Support/Firefox` y los manifiestos en `…/Application Support/Mozilla/…`, que **no
  existe hasta que alguien instala un host nativo**. La conclusión era «Firefox no está» con Firefox
  abierto delante. Ahora son dos cosas separadas —dónde se mira y dónde se escribe— y hay una prueba
  que comprueba que el manifiesto se escribe **aunque su carpeta no exista** (2026-09-10, 2.17.2).
- ~~Un panel que espera para siempre no dice nada~~ → el trabajador de fondo promete contestar más
  tarde, y si su respuesta no llegaba nunca el panel se quedaba mudo, idéntico a estar roto. Ahora
  dice «Preguntando…» desde el primer instante, tiene cinco segundos de plazo y enseña **su propia
  versión**, que zanja la duda de qué build se está mirando sin tener que preguntarla
  (2026-09-10, 2.17.2).
- ~~La extensión no hacía nada en Firefox~~ → el panel enseñaba el sitio y debajo, nada. Dos cosas.
  La primera, un error de bulto: el espacio de nombres `chrome.*` **devuelve promesas en Chrome y
  retrollamadas en Firefox**, así que un `await chrome.runtime.sendMessage(…)` allí recibe
  `undefined` y lo siguiente revienta. Se usa `browser` si está, que en los dos casos da promesas. Y
  la segunda, la que lo hizo invisible: **nada recogía ese fallo**, así que el panel se quedaba en
  blanco sin decir por qué. Ahora cualquier fallo se enseña ahí mismo. Lo vio el cliente al primer
  intento (2026-09-10, 2.17.1).
- ~~El canal con el navegador no funcionaba en Windows~~ → nació así el mismo día. Se sopesó una
  tubería con nombre —que es lo que usa KeePassXC allí— y se descartó: sus tres ventajas son saber
  quién se conecta —que Esfinge no comprueba en **ningún** sistema, porque la forma de hacerlo es la
  de 1Password y exige estar firmado—, un descriptor de seguridad de solo el dueño —que ya lo da el
  perfil del usuario— y evitar que otro se adelante —que se consigue preguntando—. A cambio pedía
  doscientas líneas de llamadas al sistema de Windows **en la frontera de seguridad de un gestor de
  contraseñas y escritas en una máquina donde no se pueden ejecutar**, que es exactamente cómo se
  rompió la 2.9.1. Se usa el mismo socket, que Go admite en Windows desde la 10.0.17063
  (2026-09-10).
- ~~Los vectores fijos del formato no estaban en el repositorio~~ → el `.gitignore` lleva `*.esf`, que
  es lo razonable en un programa que produce ficheros con esa extensión, y **se tragó exactamente los
  quince vectores**, que tienen esa extensión. Todo lo que promete la [ADR 0022](adr/0022-vectores-fijos.md)
  era cierto solo en el disco de desarrollo, y los tres `v15-*` —sellados con el compilador de la
  1.5.0, con sal y nonce de verdad— **no se pueden volver a fabricar**: se habrían perdido con la
  máquina. Lo decía GitHub en rojo desde la 2.14.0 (2026-09-10, sin versión nueva: no cambia nada del programa).
- ~~Publicar no comprobaba nada~~ → los tests los corría solo el flujo «Compilar», en paralelo y sin
  que nadie dependiera de él, así que **una versión salía igual con las pruebas en rojo**, y salieron
  seis. Ahora «Publicar» los corre como puerta: si fallan, no se compila ni se publica. Y «Compilar»
  deja de dispararse en las etiquetas, que era trabajo duplicado y el correo de fallo que llegaba al
  cliente (2026-09-10, sin versión nueva).
- ~~Lo borrado no bloquea ya su propia reimportación~~ → lo trajo la papelera de la 2.16.0: con la
  entrada entera guardada dentro, el índice de duplicados del importador la reconocía y volver a
  pasar el CSV la daba por repetida. Se habría visto como «la borré, la reimporté y no ha vuelto»,
  con la única copia escondida en la papelera y a punto de caducar. El índice ignora lo que está en
  la papelera (2026-09-10, 2.16.0).
- ~~Borrar una nota segura o una tarjeta dejaba su contenido dentro del fichero~~ → el borrado suave
  limpiaba la contraseña, el TOTP y el historial, que es el secreto de **una credencial**. El texto de
  una nota segura, el número de una tarjeta, su código de verificación y el número de un documento
  —que son el secreto entero de esas otras tres clases— se quedaban tal cual, para siempre y sin
  forma de sacarlos. Había dos listas de campos sensibles en dos sitios y solo una estaba completa;
  ahora hay una (2026-09-10, 2.15.0). **Lo que arregló esto duró unas horas y quedó sustituido por la
  papelera de la 2.16.0**, que guarda lo borrado a propósito porque ya se puede vaciar; el fallo de
  fondo —dos listas de campos sensibles— sigue arreglado.
- ~~El goteo de iconos no guardaba nada hasta terminar la tanda~~ → casi cuatro minutos antes de que
  apareciera el primero, y quien cerraba la bóveda antes no se llevaba ninguno ni de los ya
  descargados. Se guarda y se avisa uno a uno (2026-09-09, 2.14.2).
- ~~La descarga de iconos no funcionaba en absoluto~~ → el filtro de direcciones privadas estaba en
  `DialContext`, que recibe el nombre sin resolver: `ParseIP("github.com")` da nulo y se rechazaban
  todos los sitios del mundo. La 2.14.0 salió así y no descargó ni un icono. Lo dijo el cliente
  (2026-09-09, 2.14.1).
- ~~Un repositorio de terceros roto dejaba sin publicar~~ → la máquina de GitHub trae listas de apt
  que esta compilación no necesita, y `apt-get update` falla entero si cualquiera sirve un índice
  caducado. Un problema en un servidor de Google dejó sin publicar la 2.14.0. Ahora se quitan antes
  de mirar (2026-09-09, 2.14.0).
- ~~`ESFINGE_SIN_RED` solo la miraba la línea de comandos~~ → la ventana no la consultaba nunca, así
  que quien la ponía creyendo que apagaba la red apagaba la mitad. Con una segunda salida eso dejó de
  ser aceptable: vive en `internal/red` y hay una prueba por cada salida (2026-09-09, 2.14.0).
- ~~La bóveda no tenía cerrojo~~ → ya había dos gorrutinas tocándola: la que atiende a la ventana y
  el tic del bloqueo, que llama a `Cerrar()` justo mientras `Guardar` puede estar serializando. Era
  una ventana estrecha que nadie había pillado, y el trabajo de fondo la volvía ancha (2026-09-09,
  2.14.0).
- ~~Reimportar el mismo fichero duplicaba la bóveda entera~~ → los repetidos se marcaban pero se
  metían igual, así que pasar dos veces el `credentials.csv` dejaba 130 entradas donde había 65. Lo
  contó el cliente después de hacerlo. Ahora una entrada idéntica no se mete, y solo entra —marcada—
  la cuenta que ya estaba **con otra contraseña** (2026-09-09, 2.12.6).
- ~~Una lectura de preferencias que llegaba tarde deshacía un cambio~~ → la respuesta de una lectura
  pedida antes de un cambio llegaba después y se aplicaba encima, con lo que el cambio siguiente
  partía de lo viejo y borraba el anterior. Cambiar dos ajustes seguidos dejaba el primero como
  estaba (2026-09-09, 2.12.6).
- ~~Un `<strong>` dentro de un aviso partía la frase en columnas~~ → los avisos y las notas eran
  contenedores flexibles, para colocar el glifo delante, así que cada trozo del párrafo se convertía
  en un elemento por su cuenta. Era así desde el principio y no se notó mientras todos fueron texto
  pelado. Ahora es una sangría francesa (2026-09-09, 2.12.5).
- ~~No se podía borrar la bóveda desde el programa~~ → había que ir al Finder y borrar dos ficheros,
  y quien borraba solo el primero se dejaba una bóveda entera en el `.anterior` (2026-09-09, 2.12.5).
- ~~Las tarjetas y los documentos de Dashlane no se podían importar~~ → el importador tenía una sola
  tabla de alias, y una misma columna significa cosas distintas en cada uno de los cinco ficheros que
  exporta Dashlane. Sus `payments.csv` e `ids.csv` se rechazaban enteros. Ahora se reconoce la forma
  del fichero antes de mapear nada (2026-09-09, 2.12.4).
- ~~Todas las tarjetas se marcaban como duplicadas entre sí~~ → la huella de una entrada era «sitio
  más usuario» y una tarjeta no tiene ninguno de los dos, así que todas tenían la misma. Lo mismo con
  las notas seguras. Cada clase se identifica ahora por lo suyo (2026-09-09, 2.12.4).
- ~~Lo exportado no volvía a entrar entero~~ → el CSV de salida solo llevaba los campos de una
  credencial, así que exportar y reimportar perdía tarjetas y documentos (2026-09-09, 2.12.4).
- ~~Cambiar dos ajustes seguidos perdía el primero~~ → Go recibe el objeto entero y el segundo cambio
  partía del estado de React de antes de que se redibujara, así que deshacía el primero. En pantalla
  los dos se veían puestos. Lo encontró una prueba (2026-09-09, 2.12.4).
- ~~El diálogo de abrir no dejaba elegir nada en macOS~~ → el filtro «todos los ficheros» iba con el
  patrón `*.*`, que es lo idiomático en Windows; Wails le quita el `*.` de delante antes de dárselo
  al `NSOpenPanel`, así que llegaba como una extensión llamada `*` y el panel lo dejaba todo en gris.
  En macOS no se manda ya ningún filtro. Estuvo así desde que existen los diálogos y no se vio nunca
  porque allí los ficheros se arrastran; lo encontró el cliente al ir a importar de Dashlane, que es
  lo primero que no tiene arrastrar y soltar (2026-09-09, 2.12.3).
- ~~Pegar con ⌘V no activaba el botón de cifrar~~ → React no ve un `campo.value = …`: mantiene su
  propio registro del valor en un accesor del elemento, así que al asignar directamente el evento
  `input` posterior no le parece un cambio y no llama a `onChange`. La clave se veía en pantalla y
  para la aplicación el campo seguía vacío. Afectaba a **todos** los campos, porque los menús son
  propios y ⌘V pasa por `ordenes.ts`. Lo dijo el cliente (2026-09-09, 2.12.1).
- ~~La clave generada en Cifrar no se podía copiar~~ → de un campo `type="password"` el navegador se
  niega a copiar, sin decirlo: `execCommand("copy")` devuelve que sí y no copia nada. Ahora se copia
  por Go —que además arma el borrado del portapapeles— y el campo tiene un ojo para destaparla
  (2026-09-09, 2.12.1).
- ~~Un `EventSource` por oyente en el puente~~ → el navegador solo abre **seis conexiones** contra el
  mismo origen y un flujo de eventos no termina nunca, así que a partir del sexto oyente **toda
  llamada al puente se quedaba esperando para siempre**, sin error y sin petición en la red. Con
  cinco la aplicación funcionaba; la bóveda trajo el sexto. Ahora hay una sola fuente para todos
  (2026-09-09, 2.12.0).
- ~~Un guardado de preferencias a medias apagaba el bloqueo de la bóveda~~ → `GuardarPreferencias`
  recibe el objeto entero, así que un objeto incompleto llegaba con los plazos a cero; con el cero
  significando «nunca», eso apagaba en silencio el bloqueo y el borrado del portapapeles. Ahora
  «nunca» es `-1` y el cero conserva lo que hubiera (2026-09-09, 2.12.0).
- ~~El portapapeles no se limpiaba nunca~~ → desde la 2.8.0, «Usar como clave» copiaba una contraseña
  generada en claro y ahí se quedaba, cosa que `docs/seguridad.md` reconocía sin resolver. Ahora lo
  borra un reloj de Go —no un temporizador del webview, que se pausa y muere al recargar— y **nunca
  pisa lo que se haya copiado después** (2026-09-09, 2.12.0).
- ~~El arrastrar y soltar no hacía nada~~ → el modo «zona» de Wails exige declarar una propiedad CSS
  que no se estaba declarando (2026-09-07, 2.0.1).
- ~~Los símbolos de SF Symbols salían como cuadro vacío fuera de macOS~~ → `@supports` no sabe
  detectar fuentes; se volvió a caracteres normales (2026-09-07, 2.0.1).
- ~~El arranque tardaba cinco segundos en terminales que no contestan al OSC 11~~ → lo causaba el
  `init()` de Bubble Tea, que se fue con la interfaz de terminal (2026-09-07, 2.0.0).

| ~~Cambiar de sección borra lo escrito~~ | Media | Cada sección se desmontaba al salir y con ella se iba lo escrito | **Saldada en la 2.9.0**: las secciones se quedan montadas desde la primera visita y se esconden en vez de quitarse (2026-09-07) |

| ~~Medir el vidrio sin romper la aplicación~~ | Alta | El diagnóstico de la 2.9.1 dejaba la aplicación cerrándose sola al arrancar. Tres cosas de ese Objective-C pueden reventar y ninguna avisa al compilar: devolver el `UTF8String` de una cadena autoliberada, que `alphaComponent` lanza excepción sobre un color de patrón, y que `valueForKey:@"drawsBackground"` puede no existir para lectura. Revertido en la 2.9.2 | **Saldada en la 2.10.0, y no midiendo sino leyendo**: el código de Wails está en el caché de módulos de esta máquina, y ahí se ve que nunca le pone material al `NSVisualEffectView`. No hacía falta instrumentar la aplicación del cliente para averiguarlo (2026-09-07) |

| ~~Adivinar en macOS sale caro y no hay banco de pruebas~~ | Media | Tres versiones seguidas —2.5.1, 2.9.0 y 2.9.1— salieron con un cambio de macOS que no se podía probar aquí, y las tres fallaron: dos no hicieron nada visible y la tercera cerró la aplicación, dejando al cliente sin herramienta hasta la 2.9.2. El ciclo «publicar para ver qué pasa» lo sufre él, no yo | **Saldada (2026-09-08)**: `compilar.yml` acepta `inspector` en su disparador manual y compila con `wails build -devtools`, que deja el Web Inspector en el paquete. Cada hipótesis pasa a costar una línea en una consola en vez de una versión publicada. Marcada por tres sitios para que no se confunda con una compilación normal: nombre del artefacto, retención y sufijo en la versión |
