{
  "name": "esfinge-navegador",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b && vite build && vite build --config vite.fondo.config.ts",
    "comprobar": "tsc -b --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.9.0",
    "vite": "^6.0.0"
  }
}
